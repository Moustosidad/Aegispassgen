[project]
name = "aegispassgen"
version = "2.0.0"
description = "Advanced password generation engine for security auditing"
requires-python = ">=3.9"

[project.scripts]
aegispassgen = "aegispassgen.cli:main"
